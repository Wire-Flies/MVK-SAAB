const email = "Saab2@blufffmail.com";
const password = "Saab2123";
var userId, anomalies;

// initiate firebase connection
initFireBase();
signInFirebase();

// cesium stuff
var viewer = new Cesium.Viewer('cesiumContainer');

function spawnAnomalies() {
    // create anoamlies with cesium
}

